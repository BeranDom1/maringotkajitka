RewriteEngine On

# Use one canonical HTTPS hostname for every public URL.
RewriteCond %{HTTPS} !=on [OR]
RewriteCond %{HTTP_HOST} ^www\.maringotkauvody\.cz$ [NC]
RewriteRule ^ https://maringotkauvody.cz%{REQUEST_URI} [R=301,L,NE]
